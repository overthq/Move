query Tickets($userId: ID!) {
  tickets(userId: $userId) {
    _id
    userId
    route {
      _id
      origin {
        _id
        name
      }
      destination {
        _id
        name
      }
      fare
    }
    quantity
  }
}
